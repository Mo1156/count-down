// create a for loop
for (let index = 0; index <= 3; index++) {
    game.splash("counting up:" + index)
}
// create a for loop that counts to 10 
for (let index = 0; index <= 10; index++) {
    game.splash("counting up:" + index)
}
// create a for loop that counts down 100 to 0 
for (let index = 100; index >= 0; index--) {
    game.splash("counting down:" + index)
}
// create a for loop that counts up 100 skiping 10 numbers each time 
for (let index = 100; index <= 0; index-=10) {
    game.splash("counting up by 10:" + index)
}